include ./blockdraw.mpdf

/* Steps to reproduce:
   1) Remove the label files.
   2) Change the TeX strings to lower case letters and compile.
   3) Change to upper case and compile.
   4) Change to lower case and compile.
*/

[draw [TeX `$w_1$´]]
[draw [TeX `$w_2$´]]
